# Missing Model Concepts

These 2D low-poly references provide the intended look for the two catalog placeholders that did not have a matching supplied optimized model:

- `desert-pearl-concept.png` → `models/desert_pearl.glb`
- `pinecone-concept.png` → `models/pinecone.glb`

Both concepts use the same warm studio backdrop and faceted, cozy game-art style as the broader prop-sheet library.
